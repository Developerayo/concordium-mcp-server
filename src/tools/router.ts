import { Router } from "express";

export const toolRouter = Router();

// PR1: stub only — mounts cleanly for /tools path.
// PR2 will register new MCP tools here (getFinalizedTransfers, resolvePlt, etc.).
